plugin: nutanix.ncp.ntnx_prism_vm_inventory
host: "{{ lookup('env', 'NUTANIX_HOST') }}"
username: "{{ lookup('env', 'NUTANIX_USERNAME') }}"
password: "{{ lookup('env', 'NUTANIX_PASSWORD') }}"
validate_certs: "{{ lookup('env','NUTANIX_VALIDATE_CERTS') | default('false') | bool }}"
compose:
  ansible_host: ip_address

